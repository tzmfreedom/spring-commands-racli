RSpec.describe Spring::Commands::Racli do
  it "has a version number" do
    expect(Spring::Commands::Racli::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
